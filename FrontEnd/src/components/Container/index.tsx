import styled from "styled-components";

export const Container = styled.section`
  background-image: linear-gradient(
    var(--cor-principal) 90%,
    var(--background-theme)
  );
  padding: 2em;
  height: 100%;
`;
